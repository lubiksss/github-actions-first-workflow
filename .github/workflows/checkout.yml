name: Our Workflow
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          repository: 'lubiksss/KPT0-crawling-templ'
          token: ${{ secrets.GH_TOKEN }}
      - run: echo ${{github.ref_name}}
      - run: ls -al

